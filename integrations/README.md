# Integrations

This folder keeps the Aria PC completion markers in a machine-readable form.

`status.json` is intentionally evidence-first: an integration is marked complete only when a current local or connected-service check proves it. Services with `target_missing` still need a concrete workspace, repository, channel, page, base, project, or deployment target before the overall goal can be closed.
